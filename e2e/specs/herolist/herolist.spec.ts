'use strict';

//import { HeroListService } from '../../../app/components/herolist/herolist.service';

describe('HeroList Component', () => {
    it('Test HeroList', () => {
        expect(true).toEqual(false);
    });
    
    it('HeroList is not true', () => {
        expect(false).toEqual(true);
    });
    
    it('Test HeroList is true', () => {
        expect(true).toEqual(true);
    });
    
    it('Test HeroList false', () => {
        expect(false).toEqual(false);
    });
    
});

describe('HeroList Service', () => {
    it('Test HeroList Service 1', () => {        
        expect(false).toEqual(true);
    });
    
    it('Test HeroList Service 2', () => {
        expect(true).toEqual(true);
    });
})

